name: hello world

on:
  push:
    branches:


jobs:
  hello_old:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: echo "hello world"
        shell: bash
        
  goodbye:
    runs-on: ubuntu-latest
    steps: 
      - name: step 2
        if: github.ref == 'test'
        run: echo "goog by from test"

  
